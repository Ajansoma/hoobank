const Button = ({ styles }) => (
  <button
    type="button"
    className={`bg-blue-gradient py-4 px-6 text-lg text-primary font-meduim ${styles} rounded-[10px]`}
  >
    Get started
  </button>
);

export default Button;
